// Endpoint coordinates now derive from the worldwide airport dataset (see
// `airports.ts`), so they cover virtually every commercial airport rather than
// a hand-maintained shortlist. Re-exported here to keep existing imports stable.
export { AIRPORT_COORDS, CITY_COORDS } from './airports';
